const [a, b, c, d] = [3, 5, 1, 15];

//operadores aritméticos:
const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao =  d / a;
const restoOuModulo = a % 2;
console.log(soma, subtracao, multiplicacao, divisao, restoOuModulo);
//transforma o resultado em negativo: 
console.log(-soma, -subtracao, -multiplicacao, -divisao, -restoOuModulo);